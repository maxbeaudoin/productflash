// Drizzle schema for Product Flash.
// Tables defined per SCOPE.md §5 will land in task #2.
// This file intentionally exports nothing yet — drizzle-kit needs a real
// schema module to point at, and an empty module keeps the config valid.
export {}
